<div>
  <%= flash[:notice] %>
</div>

<h1>全ユーザー一覧</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %> - <%= user.email %>
      <!-- リンクを追加 -->
      <%= link_to "詳細", admin_user_path(user.id) %>
    </li>
  <% end %>
</ul>