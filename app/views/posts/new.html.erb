<h1>Posts#new</h1>
<%= form_with model: @post do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Content</h4>
  <%= f.text_area :content %>
  <h4>Image</h4>
  <%= f.file_field :image, accept: "image/*" %>

  <%= f.submit 'POST' %>

<% end %>
